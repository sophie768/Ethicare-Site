# Hobart photography — audit notes (5 Aug 2026)

- **`hobart-tasman-bridge.jpg`** and **`hobart-mona.jpg`" have swapped contents relative to their
  filenames. `hobart-tasman-bridge.jpg` is actually Mona's excavated sandstone gallery interior;
  `hobart-mona.jpg` is actually a bridge at night. Both were live and wrong (see `assets/tas/README.md`
  for the fix history). Do not use either filename at face value — check the file before reusing.
- Everything captioned "Tasman Bridge" across the site now points at the verified genuine
  `assets/tas/tas-the-iconic-tasman-bridge-at-2377136059.jpg`. Nothing currently points at
  `hobart-mona.jpg` or `hobart-tasman-bridge.jpg`; leave them alone until renamed/re-verified.
- `hobart-map.jpg` and `hobart-battery-point.jpg` are fine (verified in the full library audit).
